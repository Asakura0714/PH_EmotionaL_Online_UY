using UnityEngine;
using UnityEngine.UI;
using TMPro;

public class CJ_UI_Counter : MonoBehaviour
{
    [SerializeField] private Button _plusButton = default;
    [SerializeField] private Button _minusButton = default;
    [SerializeField] private TextMeshProUGUI _counterDisplayText = default;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
